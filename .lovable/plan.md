
# Re-post the NSE Quant Engine download

No code changes. Just re-surface the existing zip from my sandbox (`/mnt/documents/nse_quant_engine_v4_2_patched.zip`, 16 MB, last built earlier this turn) as a fresh download card in the next chat message, with a one-line reminder of the Windows steps:

1. Click the card → save the zip.
2. Unzip it.
3. Double-click `setup_windows.bat` (one-time).
4. Double-click `run_app.bat` → click **Run**.

If you'd rather I rebuild the zip from scratch first (e.g. you suspect the cached one is stale), say so and I'll regenerate it before re-posting.
