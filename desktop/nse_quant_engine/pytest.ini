[pytest]
# Only the maintained test package is collected. Standalone operator scripts
# (e.g. amfi_metadata_smoke.py) are executables, not tests, and must never be
# imported during collection.
testpaths = tests
python_files = test_*.py
